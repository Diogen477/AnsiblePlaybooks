# Ansible Playbook для настройки окружения GitLab Runner с 1C:Enterprise

Автоматизированная установка и настройка окружения для работы с GitLab Runner, включая 1C:EDT, платформу 1С, Java, SonarQube Scanner и инструменты удаленного доступа.

## 📋 Содержание

- [Описание](#описание)
- [Требования](#требования)
- [Установленные компоненты](#установленные-компоненты)
- [Структура проекта](#структура-проекта)
- [Быстрый старт](#быстрый-старт)
- [Настройка](#настройка)
- [Использование](#использование)
- [Безопасность](#безопасность)
- [Возможные проблемы](#возможные-проблемы)

## 📖 Описание

Данный Ansible playbook предназначен для автоматического развертывания полноценного окружения GitLab Runner с поддержкой разработки на платформе 1С:Enterprise. Playbook устанавливает все необходимые зависимости, инструменты разработки, среду выполнения и настраивает удаленный доступ.

**Целевое применение:**
- Настройка CI/CD для проектов 1С:Enterprise
- Подготовка виртуальных машин для GitLab Runner
- Автоматизация развертывания окружений разработки

## ⚙️ Требования

### Системные требования

- **ОС**: Ubuntu 20.04/22.04 или Debian 11/12
- **Архитектура**: x86_64
- **RAM**: Минимум 4 GB (рекомендуется 8 GB)
- **Диск**: Минимум 20 GB свободного пространства
- **Права**: root или sudo доступ

### Требования к Ansible

```bash
# Минимальная версия
ansible >= 2.12

# Рекомендуемая версия
ansible >= 2.15
```

### Дополнительные зависимости

```bash
# На управляющей машине
python3 >= 3.8
ansible-core >= 2.12
```

### Учетные данные 1С ИТС

Для загрузки дистрибутивов 1C:EDT и 1C:Platform требуется:
- Логин на портале releases.1c.ru
- Пароль от учетной записи ИТС

## 📦 Установленные компоненты

### Базовые пакеты
- **Системные утилиты**: ssh, sshpass, unzip, screen, curl, wget
- **Графическая оболочка**: Xorg, OpenBox, Xvfb
- **Удаленный доступ**: xRDP

### Среда разработки
- **Java**: Azul Zulu 17 JDK
- **1C:EDT**: 1C:Enterprise Development Tools (версия из переменных)
- **1C:Platform**: Платформа 1С:Предприятие 8.3 (версия из переменных)

### Инструменты качества кода
- **SonarQube Scanner**: Статический анализ кода

## 📁 Структура проекта

```
ansible_for_GitLabRunner/
├── ansible.cfg              # Конфигурация Ansible
├── inventory                # Инвентарный файл (localhost)
├── site.yml                 # Главный playbook
├── group_vars/
│   └── all.yml             # Глобальные переменные
└── roles/
    ├── common/             # Базовая настройка системы
    │   └── tasks/
    │       └── main.yml
    ├── java/               # Установка Java
    │   └── tasks/
    │       └── main.yml
    ├── edt/                # Установка 1C:EDT
    │   └── tasks/
    │       └── main.yml
    ├── platform/           # Установка 1C:Platform
    │   └── tasks/
    │       └── main.yml
    ├── sonar-scanner/      # Установка SonarScanner
    │   └── tasks/
    │       └── main.yml
    └── rdp-tools/          # Настройка RDP доступа
        └── tasks/
            └── main.yml
```

## 🚀 Быстрый старт

### 1. Клонирование репозитория

```bash
git clone https://github.com/Diogen477/AnsiblePlaybooks.git
cd AnsiblePlaybooks/ansible_for_GitLabRunner
```

### 2. Настройка учетных данных

**⚠️ ВАЖНО**: Не используйте значения по умолчанию!

Отредактируйте файл `group_vars/all.yml`:

```yaml
# Учётные данные для портала 1С (замените на реальные!)
releases_login: "ваш_логин@example.com"
releases_pass: "ваш_пароль"

# Версии продуктов (можно изменить при необходимости)
edt_version: "2024.2.6"
platform_version: "8.3.25.1633"
sonar_scanner_version: "5.0.1.3006"

# Настройки RDP
rdp_port: 3389
```

**Рекомендуется** использовать Ansible Vault для защиты credentials:

```bash
# Создание зашифрованного файла
ansible-vault create group_vars/vault.yml

# Добавьте в vault.yml:
vault_releases_login: "ваш_логин"
vault_releases_pass: "ваш_пароль"

# Измените all.yml:
releases_login: "{{ vault_releases_login }}"
releases_pass: "{{ vault_releases_pass }}"
```

### 3. Проверка playbook

```bash
# Проверка синтаксиса
ansible-playbook site.yml --syntax-check

# Dry-run (проверка без изменений)
ansible-playbook site.yml --check

# Проверка с подробным выводом
ansible-playbook site.yml --check -vv
```

### 4. Запуск установки

```bash
# Базовый запуск
ansible-playbook site.yml

# С использованием vault
ansible-playbook site.yml --ask-vault-pass

# С конкретными тегами (только RDP)
ansible-playbook site.yml --tags rdp

# Пропуск конкретных ролей
ansible-playbook site.yml --skip-tags rdp
```

## 🔧 Настройка

### Изменение версий компонентов

В файле `group_vars/all.yml` можно изменить версии устанавливаемых компонентов:

```yaml
# Актуальные версии можно найти на:
# EDT: https://releases.1c.ru/project/DevelopmentTools10
# Platform: https://releases.1c.ru/project/Platform83

edt_version: "2024.2.6"           # Версия EDT
platform_version: "8.3.25.1633"   # Версия платформы
sonar_scanner_version: "5.0.1.3006"  # Версия SonarScanner
```

### Настройка RDP

```yaml
# Порт для RDP подключений
rdp_port: 3389  # Стандартный порт, можно изменить

# После установки подключайтесь через:
# xfreerdp /v:server_ip:3389 /u:root
```

### Выборочная установка компонентов

Закомментируйте ненужные роли в `site.yml`:

```yaml
---
- name: Установка инфраструктуры на локальной машине
  hosts: local
  connection: local
  become: yes
  roles:
    - role: common
    - role: java
    # - role: edt          # Отключить EDT
    - role: platform
    - role: sonar-scanner
    # - role: rdp-tools    # Отключить RDP
```

## 💻 Использование

### Выполнение на удаленном хосте

Измените `inventory`:

```ini
[gitlab_runners]
runner1.example.com ansible_user=ubuntu ansible_become=yes

[gitlab_runners:vars]
ansible_python_interpreter=/usr/bin/python3
```

Запуск:

```bash
ansible-playbook -i inventory site.yml
```

### Проверка установленных компонентов

После успешной установки проверьте компоненты:

```bash
# Java
java -version

# 1C:EDT
ls -la /opt/1c/edt/

# 1C:Platform
ls -la /opt/1cv8/

# SonarScanner
sonar-scanner --version

# RDP
systemctl status xrdp
```

### Подключение через RDP

```bash
# Linux (xfreerdp)
xfreerdp /v:server_ip:3389 /u:root

# Windows
mstsc /v:server_ip:3389

# macOS (Microsoft Remote Desktop)
# Используйте GUI приложение
```

## 🔐 Безопасность

### ⚠️ Важные предупреждения

1. **Credentials в открытом виде**
   - Используйте Ansible Vault для защиты паролей
   - Не комитьте `group_vars/all.yml` с реальными credentials в Git

2. **Root доступ через RDP**
   - Playbook разрешает RDP доступ для root
   - **Рекомендуется**: создать отдельного пользователя и отключить root RDP

3. **Firewall не настраивается**
   - После установки настройте firewall вручную:
   ```bash
   sudo ufw allow 3389/tcp  # RDP
   sudo ufw enable
   ```

### Рекомендации по безопасности

```bash
# 1. Смените пароль root после установки
sudo passwd root

# 2. Создайте отдельного пользователя для RDP
sudo adduser gitlab-runner
sudo usermod -aG sudo gitlab-runner

# 3. Отключите root RDP (отредактируйте /etc/xrdp/sesman.ini)
sudo sed -i 's/AllowRootLogin=true/AllowRootLogin=false/' /etc/xrdp/sesman.ini
sudo systemctl restart xrdp

# 4. Настройте SSH ключи вместо паролей
```

### Защита credentials с Ansible Vault

```bash
# Создание vault файла
ansible-vault create group_vars/vault.yml

# Редактирование vault файла
ansible-vault edit group_vars/vault.yml

# Запуск с vault
ansible-playbook site.yml --ask-vault-pass

# Использование файла с паролем
echo "vault_password" > .vault_pass
ansible-playbook site.yml --vault-password-file .vault_pass
```

## 🐛 Возможные проблемы

### Ошибка загрузки дистрибутивов 1C

**Проблема**: `Не удалось найти ссылку для скачивания`

**Причины**:
- Неверные учетные данные ИТС
- Указана несуществующая версия
- Проблемы с доступом к releases.1c.ru

**Решение**:
```bash
# Проверьте логин и пароль
# Проверьте доступность версии на https://releases.1c.ru
# Попробуйте другую версию в group_vars/all.yml
```

### RDP не запускается

**Проблема**: `xrdp service failed to start`

**Решение**:
```bash
# Проверьте логи
sudo journalctl -u xrdp -n 50

# Проверьте конфигурацию
sudo xrdp-sesman --config /etc/xrdp/sesman.ini --test

# Переустановите xrdp
sudo apt remove --purge xrdp
sudo apt install xrdp
```

### Java не устанавливается (apt_key deprecated)

**Проблема**: `apt_key module is deprecated`

**Решение**: Используйте обновленную версию модуля или Ansible >= 2.15

```yaml
# Современный подход (требует доработки роли):
- name: Импорт GPG-ключа Azul
  ansible.builtin.get_url:
    url: https://repos.azul.com/azul-repo.key
    dest: /etc/apt/trusted.gpg.d/azul.asc
```

### Недостаточно места на диске

**Проблема**: `No space left on device`

**Решение**:
```bash
# Проверьте свободное место
df -h

# Очистите временные файлы
sudo apt clean
sudo apt autoremove

# Требуется минимум 20 GB свободного места
```

### Ошибка "Connection refused" при тестировании портов

**Проблема**: wait_for таймаут

**Решение**:
```bash
# Проверьте что служба запущена
sudo systemctl status xrdp

# Проверьте что порт открыт
sudo netstat -tulpn | grep 3389

# Проверьте firewall
sudo ufw status
```

## 📝 Примечания

### Время выполнения

- **Полная установка**: ~20-30 минут (зависит от скорости интернета)
- **Без EDT/Platform**: ~5-10 минут

### Размер дистрибутивов

- 1C:EDT: ~1.5 GB
- 1C:Platform: ~500 MB
- Все пакеты: ~3 GB

### Совместимость

Протестировано на:
- ✅ Ubuntu 20.04 LTS
- ✅ Ubuntu 22.04 LTS
- ⚠️ Debian 11 (требует дополнительной настройки)

## 🤝 Участие в разработке

Если вы нашли баг или хотите предложить улучшение:

1. Создайте Issue на GitHub
2. Предложите Pull Request с исправлением
3. Опишите изменения и их цель

## 📄 Лицензия

Этот проект распространяется "как есть" без каких-либо гарантий.

## ✍️ Автор

[Diogen477](https://github.com/Diogen477)

## 🔗 Полезные ссылки

- [Документация Ansible](https://docs.ansible.com/)
- [1C:Enterprise Downloads](https://releases.1c.ru/)
- [SonarQube Scanner](https://docs.sonarqube.org/latest/analysis/scan/sonarscanner/)
- [xRDP Documentation](http://xrdp.org/)

---

**Последнее обновление**: Февраль 2026
