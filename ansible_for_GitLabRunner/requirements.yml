---
# Минимальные требования для запуска playbook
collections:
  - name: community.general
    version: ">=5.0.0"
    source: https://galaxy.ansible.com
  
  - name: ansible.posix
    version: ">=1.4.0"
    source: https://galaxy.ansible.com

# Установка коллекций:
# ansible-galaxy collection install -r requirements.yml
